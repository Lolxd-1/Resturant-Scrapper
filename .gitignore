# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local Zomato page cache (re-downloaded automatically, never commit)
zcache/
*.html

# Per-run outputs (each run writes its own --out dir; never commit)
zimages/
previews/
smartbiz_out*/
_smoke/
results*.json
picks*.json
novel_list.txt
smartbiz_UPLOAD_READY.xlsx
smartbiz_NOVEL_PENDING.xlsx

# Your private files - NEVER commit these
.env
.streamlit/secrets.toml
*.xlsx
# ...except the baked photo pool + default template the app needs:
!data/pairs.json
!data/smartbiz_template.xlsx
